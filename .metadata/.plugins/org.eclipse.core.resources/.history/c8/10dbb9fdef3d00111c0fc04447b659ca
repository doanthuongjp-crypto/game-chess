package com.chess.model;

public class Rook extends Piece {

    public Rook(int row, int col, boolean isWhite) {
        super(row, col, isWhite, "Rook");
    }

    @Override
    public boolean isValidMove(int targetRow, int targetCol, Piece[][] board) {
        // Quân Xe chỉ đi khi dòng cũ = dòng mới HOẶC cột cũ = cột mới
        if (this.row == targetRow || this.col == targetCol) {
            
            // Kiểm tra xem trên đường đi có quân nào cản không (Logic này sẽ phức tạp hơn một chút)
            // Tạm thời trả về true để bạn test việc di chuyển cơ bản
            return true;
        }
        return false;
    }
}